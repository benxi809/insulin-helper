name: insulin_app
description: "胰岛素剂量计算小助手 - 为糖尿病患者安全计算胰岛素用量"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  sqflite: ^2.4.2
  path: ^1.9.1
  fl_chart: ^0.70.2
  intl: ^0.20.2
  share_plus: ^10.1.4
  image_picker: ^1.1.2
  path_provider: ^2.1.5
  http: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/foods.json
